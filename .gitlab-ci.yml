image: maven:3.9.1-openjdk-11

default:
  tags:
    - shelllocal

stages:
  - test

Tests with allure:
  stage: test
  script:
    - pwd
    - mvn clean test
    - mvn allure:report allure:serve
  artifacts:
    paths:
      - target\allure-results\*