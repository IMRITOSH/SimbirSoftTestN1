image: maven:3.8-openjdk-11

default:
  tags:
    - shelllocal

stages:
  - test

Tests with allure:
  stage: test
  script:
    - mvn clean test
    - mvn allure:report allure:serve
  artifacts:
    paths:
      - target\allure-results\*